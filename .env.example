# Example environment variables for local development and CI.
# Copy this file to `.env` and fill real values. Do NOT commit `.env` to Git.

# Base
SERVER_PORT=8080
SPRING_PROFILES_ACTIVE=dev

# API keys (comma separated) - only use local/dev keys here
# For production, inject keys from a secret manager or CI secrets
API_KEYS=dev-key-1,dev-key-2

# Rate limit (tokens capacity per minute)
RATE_LIMIT_CAPACITY=120
RATE_LIMIT_REFILL_PER_MIN=120

# CORS (comma separated origins)
CORS_ALLOWED_ORIGINS=http://localhost:5173

# Dev H2 file path is configured directly in `src/main/resources/application-dev.yml`.
# If you need to override it for local development, edit `application-dev.yml` or run with the dev
# profile and set an appropriate datasource URL.

# Production DB — use environment variables or a secret manager in prod
# Example Postgres URL, username and password placeholders:
DB_URL=jdbc:postgresql://db:5432/chat
DB_USER=chat
DB_PASS=changeme

# Notes:
#  - Keep this file as a template only. Do not place real production secrets here.
#  - Your local `.env` should be added to `.gitignore` so secrets are never committed.
